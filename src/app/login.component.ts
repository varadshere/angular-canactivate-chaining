import { Component, Input } from '@angular/core';

@Component({
  selector: 'login',
  template: `<h1>Login Angular Lovers!</h1><p>Change <code>allowAccess</code> to true.  See the <code>auth-guard.servcie.ts</code></p>`
})
export class LoginComponent  {
}
